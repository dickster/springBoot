package com.brovada.document.config;

public enum InputType {
    // HTML 5 text input types.
    EMAIL, TEXT, DATE, COLOR, DATETIME, DATETIME_LOCAL, MONTH, NUMBER, RANGE, SEARCH, TEL, TIME, URL, WEEK
}
